import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "The Organized Teen | Master Your Academic Life",
  description:
    "A cohort-based webinar program that teaches high school students a practical system for managing their academic workload. Live sessions, proven templates, and a supportive peer community.",
  keywords: [
    "teen productivity",
    "study skills",
    "high school organization",
    "academic success",
    "student time management",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
